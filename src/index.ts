import defaultImportName from "./rules/default-import-name.js";

// eslint-disable-next-line import/no-unused-modules
export const rules = {
  "default-import-name": defaultImportName,
};

// eslint-disable-next-line import/no-unused-modules
export const configs = {
  recommended: {
    rules,
  },
};
